#!/usr/bin/sh

set -euf

set -a
BQ_BUMPED_VERSION=$(git cliff --bumped-version)
BQ_RELEASE_COMMIT_MESSAGE="chore(release): prepare for v${BQ_BUMPED_VERSION}"
BQ_RELEASE_TAG_MESSAGE="Release v${BQ_BUMPED_VERSION}"
set +a
